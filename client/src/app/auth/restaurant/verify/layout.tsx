import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cravon | Verify Partner",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
